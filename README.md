# 家庭维修事项

端口规划：`5114`

```bash
npm install
npm run dev
```

第一版闭环：记录维修问题、按状态查看、更新处理状态、统计未完成预计费用，支持照片链接并使用localStorage保存数据。

图片能力：新增事项时可点「选择图片」上传本地图片，自动压缩（最长边 1280、JPEG）后显示预览；非图片或超过 5 MB 会提示并保留原内容。图片以 data URL 存入 localStorage，刷新后仍显示。照片链接输入方式继续可用，两者同时填写时本地图片优先。
